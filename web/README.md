# ThreatLens web

Minimal Next.js App Router frontend. Landing page only — no dashboard, alerts, or auth. Detection is served by the API (`POST /detect`).

From this directory:

```bash
npm install
npm run dev
```

Open [http://localhost:3000](http://localhost:3000). See the repository root README for project status.
