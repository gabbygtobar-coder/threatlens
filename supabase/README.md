# Supabase (M4)

SQL migrations for ThreatLens persistence. Apply them to a Supabase project
Gabby owns — this repo does not ship live credentials.

See [docs/auth-persistence.md](../docs/auth-persistence.md) for architecture
(Option A), RLS, and local setup.
