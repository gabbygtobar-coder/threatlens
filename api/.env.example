# Comma-separated browser origins allowed to call this API (CORS).
# Local defaults are used if unset. For production, set the deployed web origin,
# e.g. https://threatlens.vercel.app — do not use * if you later send cookies.
CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000
